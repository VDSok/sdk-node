// Generated by scripts/gen.mjs from the VDSok Client API v1 OpenAPI spec.
// Source: docs/openapi/vdsok-client-api-v1.yaml
// Do not edit by hand — run `npm run gen`.
/* eslint-disable */

export type paths = {
    "/health": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Liveness and API switch state
         * @description Always `200`. `status` is `disabled` while staff have the API switched off; every other endpoint then answers `503 api_disabled`.
         */
        get: operations["get_health"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/openapi.json": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /** This document as JSON */
        get: operations["get_openapi"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/me": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Who am I
         * @description Identity of the calling key, its scopes and whether it is live or sandbox. Cheapest way to validate a key.
         */
        get: operations["get_me"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/account": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /** Account profile, client group and discount */
        get: operations["get_account"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/balance": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /** Balance and upcoming charges */
        get: operations["get_balance"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/transactions": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /** Balance transactions, newest first */
        get: operations["list_transactions"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/balance/topup-info": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /** Top-up limits and available gateways */
        get: operations["get_topup_info"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/balance/topup": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Create a top-up invoice and get a payment link
         * @description Creates an unpaid `topup` invoice and returns the gateway URL to pay
         *     it. The balance changes only after the gateway confirms the payment
         *     (event `invoice.paid`). Sandbox returns a fake `payment_url`.
         */
        post: operations["create_topup"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/invoices": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /** Invoices, newest first */
        get: operations["list_invoices"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/invoices/{invoice_id}": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                invoice_id: components["parameters"]["InvoiceId"];
            };
            cookie?: never;
        };
        /** One invoice with line items */
        get: operations["get_invoice"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/invoices/{invoice_id}/pdf": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                invoice_id: components["parameters"]["InvoiceId"];
            };
            cookie?: never;
        };
        /** Invoice as PDF */
        get: operations["get_invoice_pdf"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/invoices/{invoice_id}/pay": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                invoice_id: components["parameters"]["InvoiceId"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Pay an unpaid invoice from balance
         * @description Charges the balance synchronously and answers `200` with
         *     `status: paid`. If the invoice orders a server, provisioning runs in
         *     the background afterwards — watch it through `GET /orders` or the
         *     `server.created` webhook, the answer here does not wait for it.
         *     Insufficient balance → `402` and nothing changes.
         */
        post: operations["pay_invoice"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/invoices/{invoice_id}/payment-link": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                invoice_id: components["parameters"]["InvoiceId"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Gateway payment link for an unpaid invoice */
        post: operations["create_invoice_payment_link"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/catalog/tariffs": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * VDS tariffs available for order
         * @description Only `category = vds` and not disabled. Prices already include the caller's group discount; `list_price_monthly` is the public price.
         */
        get: operations["list_tariffs"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/catalog/tariffs/{tariff_id}": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                tariff_id: components["parameters"]["TariffId"];
            };
            cookie?: never;
        };
        /** One tariff */
        get: operations["get_tariff"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/catalog/os": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * OS images
         * @description With `tariff_id` the list excludes images that tariff forbids (`excluded_os`).
         */
        get: operations["list_os_images"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/catalog/locations": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /** Datacenter locations */
        get: operations["list_locations"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/catalog/zones": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /** Domain zones (TLDs) and prices */
        get: operations["list_zones"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/catalog/quote": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Price of an order before placing it
         * @description Runs the exact functions the order path uses (period total, group /
         *     loyalty / volume / period discounts, promo code) and reports whether
         *     the balance covers the total. Read-only: a promo code is validated but
         *     not consumed. Give either `months` or `hours`.
         */
        get: operations["get_quote"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/servers": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Servers of the account
         * @description A test key also sees its sandbox servers (`id >= 9000000000`). `ip` finds a server by any of its assigned addresses.
         */
        get: operations["list_servers"];
        put?: never;
        /**
         * Order a server
         * @description Synchronous like the cabinet: validates tariff, OS, name and capacity,
         *     computes the price, checks the balance **before writing anything**
         *     (`402` has no side effects), then charges and creates the VM. `201`
         *     carries the server and the one-time `root_password`. If the panel
         *     times out the charge is kept and `202 provisioning` is returned;
         *     poll `GET /orders/{invoice_id}` — the order either becomes `active`
         *     or is refunded automatically. Sandbox: `dry_run` validation and a
         *     fake server, with no invoice (`invoice_id` and `order_url` are
         *     `null`).
         */
        post: operations["create_server"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/servers/{server_id}": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        /**
         * Server details
         * @description Adds `refund_quote` (what `DELETE` would refund right now). `include=live` also queries
         *     the panel for power state and usage, and then the call is billed against the expensive
         *     bucket (see `x-expensive-when`). The same `409 service_state` rule as
         *     `get_server_status` applies to `include=live`: synthetic ids (`2000000000 …
         *     8999999999`) have no panel, while sandbox ids (`>= 9000000000`) return the fake
         *     snapshot.
         */
        get: operations["get_server"];
        put?: never;
        post?: never;
        /**
         * Delete the server and refund unused days
         * @description Removes the VM from the panel and credits the balance for the unused
         *     part of the paid period, computed from the invoices that were actually
         *     paid (see `GET /servers/{server_id}/refund-quote`). No refund for
         *     non-refundable (promo) tariffs, servers blocked by staff, expired
         *     ones and servers with no paid invoice behind them — the reason comes
         *     back in `refund_quote.excluded_reason`. A pending cancel request is
         *     withdrawn silently (the answer does not mention it). Panel failure
         *     other than "already gone" → `502` with nothing changed.
         */
        delete: operations["delete_server"];
        options?: never;
        head?: never;
        /** Change auto-renew, name or notes */
        patch: operations["update_server"];
        trace?: never;
    };
    "/servers/{server_id}/status": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        /**
         * Live power state and usage from the panel
         * @description Servers imported from history with synthetic ids (`2000000000 … 8999999999`, flagged
         *     `flags.synthetic: true`) have no panel record → `409 service_state`. Sandbox servers
         *     ordered with a `vk_test_` key (ids `>= 9000000000`) are the exception: they are not
         *     flagged synthetic and answer `200` with a deterministic snapshot built without ever
         *     touching the panel.
         */
        get: operations["get_server_status"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/servers/{server_id}/refund-quote": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        /** What DELETE would refund right now */
        get: operations["get_server_refund_quote"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/servers/{server_id}/renew": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Renew for months or hours from balance
         * @description Extends `billing.next_due_at`; early renewal stacks on top of the current period. Monthly servers take `months`, hourly ones take `hours`.
         */
        post: operations["renew_server"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/servers/{server_id}/actions/power": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Start, stop or restart */
        post: operations["power_server"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/servers/{server_id}/actions/reinstall": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Reinstall the OS
         * @description Destroys all data on the disk. When `password` is omitted a new one is generated and returned once.
         */
        post: operations["reinstall_server"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/servers/{server_id}/actions/reset-password": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Generate a new root password
         * @description Also limited to 5 calls per 5 minutes per server.
         */
        post: operations["reset_server_password"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/orders": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Orders still being provisioned, newest first
         * @description Only orders whose server is still being created are listed: billing
         *     has no orders table, the state lives in the invoice. A finished order
         *     is a server (`GET /servers`) and a paid invoice (`GET /invoices`).
         */
        get: operations["list_orders"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/orders/{invoice_id}": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                invoice_id: components["parameters"]["InvoiceId"];
            };
            cookie?: never;
        };
        /**
         * Order state by its invoice id
         * @description Poll after a `202 provisioning`. Terminal states are `active` (with `server_id`) and `cancelled` (charge refunded by the reconciler).
         */
        get: operations["get_order"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/servers/{server_id}/ips": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        /**
         * Addresses of the server
         * @description Read live from the panel, so the call is billed against the expensive bucket (api_v1/routes/ips.py:50).
         */
        get: operations["list_server_ips"];
        put?: never;
        /**
         * Buy an additional IPv4
         * @description Charges the prorated price for the rest of the current period (see `GET .../ips/quote`) and raises `billing.recurring_amount`. Limit per server → `409 ip_limit_reached`.
         */
        post: operations["add_server_ip"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/servers/{server_id}/ips/quote": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        /**
         * Price of one more IPv4 for this server
         * @description Asks the panel how many addresses the server already has, so the call is billed against the expensive bucket (api_v1/routes/ips.py:157).
         */
        get: operations["get_server_ip_quote"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/servers/{server_id}/ips/{ip_id}": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
                ip_id: components["parameters"]["IpId"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Release an additional IPv4
         * @description The primary address cannot be released (`409 conflict`). No refund; the recurring amount drops from the next period.
         */
        delete: operations["delete_server_ip"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/servers/{server_id}/ips/{ip_id}/ptr": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
                ip_id: components["parameters"]["IpId"];
            };
            cookie?: never;
        };
        get?: never;
        /**
         * Set or remove the PTR (reverse DNS) record
         * @description The hostname goes in `domain`. Send `""`, `null` or an empty object to
         *     remove the record. The address must have rDNS enabled in the panel,
         *     otherwise the answer is `400`.
         */
        put: operations["set_server_ip_ptr"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/ssh-keys": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * Public keys stored on the account
         * @description Listing needs `servers:read` (api_v1/routes/ssh_keys.py); adding and deleting need `servers:manage`.
         */
        get: operations["list_ssh_keys"];
        put?: never;
        /** Add a public key */
        post: operations["create_ssh_key"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/ssh-keys/{key_id}": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                key_id: components["parameters"]["SshKeyId"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Remove a public key
         * @description Does not touch servers where the key is already installed.
         */
        delete: operations["delete_ssh_key"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/domains/availability": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /** Is the name free to register */
        get: operations["check_domain_availability"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/domains": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /** Domains of the account */
        get: operations["list_domains"];
        put?: never;
        /**
         * Register a domain
         * @description Charges the balance and registers at the registrar using the
         *     account's contact profile. `201` when the registrar confirmed at
         *     once, `202 pending` when it accepted the request and the domain will
         *     become `active` on the next sync (event `domain.registered`). Taken
         *     name → `409 domain_taken`.
         */
        post: operations["register_domain"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/domains/{domain_id}": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                domain_id: components["parameters"]["DomainId"];
            };
            cookie?: never;
        };
        /** One domain */
        get: operations["get_domain"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Toggle auto-renew or WHOIS privacy
         * @description Privacy is applied at the registrar synchronously; a refusal there → `400 upstream_rejected` and nothing is saved.
         */
        patch: operations["update_domain"];
        trace?: never;
    };
    "/domains/{domain_id}/renew": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                domain_id: components["parameters"]["DomainId"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Renew for N years from balance
         * @description May answer `status = pending_sync` when the registrar accepted the renewal but the new expiry date is not yet visible; it is picked up by the daily sync.
         */
        post: operations["renew_domain"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/domains/{domain_id}/nameservers": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                domain_id: components["parameters"]["DomainId"];
            };
            cookie?: never;
        };
        get?: never;
        /** Replace the nameserver set */
        put: operations["set_domain_nameservers"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/domains/transfers": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Transfer a domain in from another registrar
         * @description Charges the transfer price (usually includes a one-year renewal) and starts the transfer; the domain appears with status `transfer_pending`.
         */
        post: operations["transfer_domain"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/keys": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /** API keys of the account */
        get: operations["list_api_keys"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/keys/{key_id}": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                key_id: components["parameters"]["ApiKeyId"];
            };
            cookie?: never;
        };
        /** One API key */
        get: operations["get_api_key"];
        put?: never;
        post?: never;
        /**
         * Revoke the calling key (kill switch)
         * @description Only the key that makes the request can be revoked here, whatever its
         *     scopes, so a leaked key can be killed from anywhere without giving
         *     every key the power to destroy the whole integration. Other keys are
         *     revoked in the cabinet. Any other `key_id` answers
         *     `403 insufficient_scope` with `details.required = ["self"]`, and a key
         *     that is already revoked answers `409 conflict`.
         *
         *     **The one mutation that is real under a test key.** Every other write
         *     is simulated for `vk_test_…` (`x-sandbox: fake`); this one is not,
         *     because a leaked test key must be killable with the leaked key itself.
         *     No money moves and nothing outside the key row is touched. The build
         *     script keeps this exception on a short allow-list, so no other
         *     mutation can quietly become real.
         */
        delete: operations["revoke_api_key"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/webhooks": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /** Webhook subscriptions */
        get: operations["list_webhooks"];
        put?: never;
        /**
         * Subscribe a URL to events
         * @description URL must be `https://` on a public IP. The `secret` is returned once; rotate it with `POST /webhooks/{webhook_id}/rotate-secret`.
         */
        post: operations["create_webhook"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/webhooks/events": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        /** Event types you can subscribe to */
        get: operations["list_webhook_events"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/webhooks/{webhook_id}": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                webhook_id: components["parameters"]["WebhookId"];
            };
            cookie?: never;
        };
        /** One subscription */
        get: operations["get_webhook"];
        put?: never;
        post?: never;
        /** Delete a subscription and its delivery log */
        delete: operations["delete_webhook"];
        options?: never;
        head?: never;
        /** Change URL, events, description; re-enable after auto-disable */
        patch: operations["update_webhook"];
        trace?: never;
    };
    "/webhooks/{webhook_id}/rotate-secret": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                webhook_id: components["parameters"]["WebhookId"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Issue a new signing secret
         * @description The old secret stops working immediately; deliveries already queued are re-signed with the new one at send time.
         */
        post: operations["rotate_webhook_secret"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/webhooks/{webhook_id}/test": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                webhook_id: components["parameters"]["WebhookId"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Send a `ping` event now and report the result
         * @description Delivered inline (not through the queue) so the answer shows the
         *     receiver's status code and latency. A disabled subscription answers
         *     `409 conflict`; if the delivery cannot be queued at all the call
         *     answers `503 temporarily_unavailable`.
         */
        post: operations["test_webhook"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/webhooks/{webhook_id}/deliveries": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                webhook_id: components["parameters"]["WebhookId"];
            };
            cookie?: never;
        };
        /**
         * Delivery log, newest first
         * @description Delivered rows are kept 14 days, dead ones 30 days.
         */
        get: operations["list_webhook_deliveries"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/webhooks/deliveries/{delivery_id}": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                delivery_id: components["parameters"]["DeliveryId"];
            };
            cookie?: never;
        };
        /**
         * One delivery with the exact payload that was sent
         * @description The only place `payload` is returned — the bytes signed and delivered, identical across retries.
         */
        get: operations["get_webhook_delivery"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/webhooks/deliveries/{delivery_id}/redeliver": {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                delivery_id: components["parameters"]["DeliveryId"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Re-queue a delivery
         * @description Sends the **same bytes** as the original event (same `id`, same `created_at`), so receivers can deduplicate by `X-Webhook-Id`.
         */
        post: operations["redeliver_webhook"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
};
export type webhooks = {
    "server.created": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** A server was created (order or paid invoice provisioned) */
        post: operations["webhook_server_created"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "server.suspended": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Server suspended (non-payment or staff action); `data.previous.status` holds the prior state */
        post: operations["webhook_server_suspended"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "server.unsuspended": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Server resumed after payment or by staff */
        post: operations["webhook_server_unsuspended"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "server.terminated": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Server deleted (by the client with refund, by cancel request, or after long non-payment) */
        post: operations["webhook_server_terminated"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "server.reinstalled": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** OS reinstall finished */
        post: operations["webhook_server_reinstalled"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "invoice.created": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** New invoice (renewal, order, top-up) */
        post: operations["webhook_invoice_created"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "invoice.paid": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Invoice paid (gateway or balance) */
        post: operations["webhook_invoice_paid"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "invoice.overdue": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Invoice past due date and still unpaid */
        post: operations["webhook_invoice_overdue"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "balance.low": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Balance dropped below the upcoming-charges threshold */
        post: operations["webhook_balance_low"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "domain.registered": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Domain became active at the registrar */
        post: operations["webhook_domain_registered"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "domain.expiring": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Domain expires soon (30, 7 and 1 day before) */
        post: operations["webhook_domain_expiring"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "domain.renewed": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Domain renewed (manually or by auto-renew) */
        post: operations["webhook_domain_renewed"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "key.created": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** A new API key was created in the cabinet */
        post: operations["webhook_key_created"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "key.revoked": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** An API key was revoked (client, self-revoke, staff or ban) */
        post: operations["webhook_key_revoked"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    ping: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Test event sent by `POST /webhooks/{webhook_id}/test` */
        post: operations["webhook_ping"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
};
export type components = {
    schemas: {
        /** @description Decimal amount as a string with 2–4 fraction digits. Never a float. */
        Money: string;
        /** @description ISO 4217 code of the account currency. */
        Currency: string;
        /**
         * Format: date-time
         * @description RFC 3339 in UTC with a trailing `Z`.
         */
        Timestamp: string;
        NullableTimestamp: components["schemas"]["Timestamp"] | null;
        /**
         * @description Permission granted to a key. Presets in the cabinet — `read_only`, `operate`, `full` — are just bundles of these.
         * @enum {string}
         */
        Scope: "account:read" | "balance:read" | "balance:topup" | "invoices:read" | "invoices:pay" | "servers:read" | "servers:manage" | "servers:order" | "servers:delete" | "domains:read" | "domains:manage" | "domains:order" | "keys:read" | "webhooks:manage";
        /** @enum {string} */
        BillingCycle: "monthly" | "hourly";
        /**
         * @description Closed list per status. Clients should still tolerate unknown codes
         *     (treat them by HTTP status).
         *
         *     400 `invalid_request` `validation_error` `invalid_cursor`
         *     `idempotency_key_required` `os_not_allowed` `invalid_period`
         *     `invalid_action` `upstream_rejected`;
         *     401 `invalid_token` `key_expired` `key_not_yet_valid`;
         *     402 `insufficient_funds`;
         *     403 `insufficient_scope` `ip_not_allowed` `account_suspended`
         *     `api_disabled_for_account` `sandbox_not_supported` `sandbox_disabled`
         *     `server_blocked` `domain_blocked`;
         *     404 `not_found`;
         *     409 `conflict` `idempotency_conflict` `idempotency_in_progress`
         *     `operation_in_progress` `service_state` `no_capacity`
         *     `tariff_unavailable` `ip_limit_reached` `domain_taken`
         *     `domain_exists` `cancel_pending`;
         *     405 `method_not_allowed` (the response also carries `Allow`);
         *     413 `payload_too_large`; 415 `unsupported_media_type`;
         *     429 `rate_limited`; 500 `server_error`; 502 `upstream_error`;
         *     503 `api_disabled` `upstream_unavailable` `temporarily_unavailable`;
         *     504 `upstream_timeout`.
         * @enum {string}
         */
        ErrorCode: "invalid_request" | "validation_error" | "invalid_cursor" | "idempotency_key_required" | "os_not_allowed" | "invalid_period" | "invalid_action" | "upstream_rejected" | "invalid_token" | "key_expired" | "key_not_yet_valid" | "insufficient_funds" | "insufficient_scope" | "ip_not_allowed" | "account_suspended" | "api_disabled_for_account" | "sandbox_not_supported" | "sandbox_disabled" | "server_blocked" | "domain_blocked" | "not_found" | "conflict" | "idempotency_conflict" | "idempotency_in_progress" | "operation_in_progress" | "service_state" | "no_capacity" | "tariff_unavailable" | "ip_limit_reached" | "domain_taken" | "domain_exists" | "cancel_pending" | "method_not_allowed" | "payload_too_large" | "unsupported_media_type" | "rate_limited" | "server_error" | "upstream_error" | "api_disabled" | "upstream_unavailable" | "temporarily_unavailable" | "upstream_timeout";
        /** @description The only error shape the API produces, including app-level 404/405/413/500. */
        Error: {
            error: {
                code: components["schemas"]["ErrorCode"];
                /** @description Human-readable, English, not for parsing. */
                message: string;
                request_id: string;
                /** @description Code-specific extras (`required` scopes, `fields`, money figures for `insufficient_funds`). */
                details?: {
                    [key: string]: unknown;
                };
            };
        };
        Health: {
            /** @enum {string} */
            status: "ok" | "disabled";
            time: components["schemas"]["Timestamp"];
            /**
             * @description API version this deployment serves (`api_v1.API_VERSION`).
             * @constant
             */
            version: "1";
        };
        Me: {
            key: components["schemas"]["ApiKey"];
            /** Format: int64 */
            account_id: number;
            /** @description False for `vk_test_…` keys — writes are simulated. */
            livemode: boolean;
            /** @description Same list as `key.scopes`. */
            scopes: components["schemas"]["Scope"][];
            /** @description Every scope this API version knows (`api_v1.auth.SCOPES`). */
            available_scopes: components["schemas"]["Scope"][];
            /** @description Whether test keys are accepted at all right now. */
            sandbox_enabled: boolean;
            /** @constant */
            api_version: "1";
        };
        ClientGroup: {
            id: number;
            name: string;
            discount_percent: number;
        };
        Account: {
            /** Format: int64 */
            id: number;
            login: string;
            /** Format: email */
            email: string;
            name?: string | null;
            company?: string | null;
            currency: components["schemas"]["Currency"];
            group: components["schemas"]["ClientGroup"] | null;
            loyalty_discount_percent?: number;
            email_verified: boolean;
            two_factor_enabled: boolean;
            created_at: components["schemas"]["Timestamp"];
        };
        Balance: {
            balance: components["schemas"]["Money"];
            currency: components["schemas"]["Currency"];
            upcoming_7d: components["schemas"]["Money"];
            upcoming_30d: components["schemas"]["Money"];
            /** @description Balance does not cover the next 7 days of renewals. */
            low_balance: boolean;
            auto_renew_total_monthly?: components["schemas"]["Money"];
        };
        Transaction: {
            /** Format: int64 */
            id: number;
            /** @enum {string} */
            direction: "credit" | "debit";
            /** @description Absolute value; see `direction`. */
            amount: components["schemas"]["Money"];
            currency: components["schemas"]["Currency"];
            /**
             * @description Derived from `transactions.gateway` plus the sign of the amount —
             *     there is no `type` column. The mapping, exhaustively:
             *
             *     * `refund` → `refund`;
             *     * `admin` → `adjustment` (staff credit or debit);
             *     * `referral` → `referral` (credit), `referral_reversal` →
             *       `referral` with `direction: debit`;
             *     * `hourly` → `payment` (an hourly charge taken by the cron);
             *     * `balance` → `payment` when the amount is negative (paid from
             *       balance) and `refund` when it is positive;
             *     * any payment-gateway name (`cryptobot`, …) → `topup`;
             *     * anything unrecognized → `other`.
             *
             *     `bonus` is reserved for promotional credits; `gateway` always
             *     carries the raw column value, so a new gateway never hides
             *     behind `other`.
             * @enum {string}
             */
            type: "topup" | "payment" | "refund" | "bonus" | "referral" | "adjustment" | "other";
            /** @description Raw `transactions.gateway` value. */
            gateway?: string | null;
            description: string;
            /** Format: int64 */
            invoice_id: number | null;
            created_at: components["schemas"]["Timestamp"];
        };
        TransactionPage: {
            data: components["schemas"]["Transaction"][];
            next_cursor: string | null;
        };
        /**
         * @description The first step of a top-up: the limits and the gateway codes the
         *     account may use right now. `gateways` is a flat list of codes — pass
         *     one of them verbatim as `TopupRequest.gateway`.
         */
        TopupInfo: {
            currency: components["schemas"]["Currency"];
            /** @description Smallest accepted top-up. */
            min: components["schemas"]["Money"];
            /** @description Largest accepted top-up. */
            max: components["schemas"]["Money"];
            /** @description Bonus added to a first top-up, in percent; `0` when the promotion is off. */
            first_topup_bonus_percent: number;
            /** @description Whether this account still qualifies for that bonus. */
            first_topup_eligible: boolean;
            /** @description Codes of the enabled payment gateways, in the order the cabinet shows them. */
            gateways: string[];
        };
        TopupRequest: {
            /** @description Also accepted as a number; the range is `min`..`max` from `GET /balance/topup-info`. */
            amount: components["schemas"]["Money"];
            /** @description One of the `gateways` strings from `GET /balance/topup-info`. */
            gateway: string;
        };
        TopupResult: {
            /** Format: int64 */
            invoice_id: number;
            /** Format: uri */
            payment_url: string;
            amount: components["schemas"]["Money"];
            currency: components["schemas"]["Currency"];
            gateway: string;
        };
        /**
         * @description Exactly the values the `invoices.status` column holds, so anything the
         *     billing system has ever written can be serialized:
         *
         *     * `not_paid` — issued, awaiting payment;
         *     * `pending` — payment started at the gateway, not confirmed yet;
         *     * `paid` — settled;
         *     * `cancelled` — withdrawn (superseded renewal, cancelled order);
         *     * `refunded` — paid and later refunded to the balance;
         *     * `failed` — paid at the gateway but the service could not be
         *       provisioned; staff refund or re-provision it manually.
         * @enum {string}
         */
        InvoiceStatus: "not_paid" | "pending" | "paid" | "cancelled" | "refunded" | "failed";
        /**
         * @description The `invoices.type` vocabulary verbatim — the same strings `?type=`
         *     filters on. Notes:
         *
         *     * `vds_addon` — an additional IPv4 (and other add-ons) for a VDS;
         *     * `domain_purchase` covers registrations and transfers;
         *     * `late_fee` — a late-payment fee inherited from the WHMCS era;
         *     * `dedicated_purchase` / `dedicated_renewal` — dedicated servers.
         *       Dedicated servers are out of v1 scope (no endpoints for them), but
         *       their invoices sit on the same account and do come back from
         *       `GET /invoices`;
         *     * `other` — invoices imported from WHMCS whose line item had no
         *       recognizable type.
         * @enum {string}
         */
        InvoiceType: "topup" | "vds_purchase" | "vds_renewal" | "vds_addon" | "domain_purchase" | "domain_renewal" | "late_fee" | "dedicated_purchase" | "dedicated_renewal" | "other";
        InvoiceItem: {
            description: string;
            amount: components["schemas"]["Money"];
            /** Format: int64 */
            server_id?: number | null;
            /** Format: int64 */
            domain_id?: number | null;
        };
        Invoice: {
            /** Format: int64 */
            id: number;
            status: components["schemas"]["InvoiceStatus"];
            type: components["schemas"]["InvoiceType"];
            amount: components["schemas"]["Money"];
            currency: components["schemas"]["Currency"];
            description: string;
            gateway: string | null;
            created_at: components["schemas"]["Timestamp"];
            due_at: components["schemas"]["NullableTimestamp"];
            paid_at: components["schemas"]["NullableTimestamp"];
            cancelled_at?: components["schemas"]["NullableTimestamp"];
            items: components["schemas"]["InvoiceItem"][];
            /** @description Relative API path of the PDF. */
            pdf_url?: string;
        };
        InvoicePage: {
            data: components["schemas"]["Invoice"][];
            next_cursor: string | null;
        };
        PayInvoiceResult: {
            /** Format: int64 */
            invoice_id: number;
            /** @constant */
            status: "paid";
            /** @description What was taken from the balance. */
            amount: components["schemas"]["Money"];
            currency: components["schemas"]["Currency"];
            /** @description The balance read just before the charge */
            balance_after: components["schemas"]["Money"];
        };
        PaymentLinkRequest: {
            /** @description Defaults to the gateway already recorded on the invoice (continuing a started payment). */
            gateway?: string;
        };
        PaymentLink: {
            /** Format: int64 */
            invoice_id: number;
            /** Format: uri */
            payment_url: string;
            gateway: string;
        };
        Resources: {
            cpu_cores: number;
            ram_mb: number;
            disk_gb: number;
            /** @description `null` = unmetered. */
            bandwidth_tb?: number | null;
            port_mbps?: number | null;
        };
        PeriodPrice: {
            /** @enum {integer} */
            months: 1 | 3 | 6 | 12;
            discount_percent: number;
            total: components["schemas"]["Money"];
        };
        Tariff: {
            id: number;
            name: string;
            description?: string | null;
            location: components["schemas"]["Location"];
            resources: components["schemas"]["Resources"];
            /** @description After the caller's group discount. */
            price_monthly: components["schemas"]["Money"];
            list_price_monthly: components["schemas"]["Money"];
            price_hourly: components["schemas"]["Money"] | null;
            currency: components["schemas"]["Currency"];
            periods: components["schemas"]["PeriodPrice"][];
            hourly_available: boolean;
            /** @description False when the location has no capacity; ordering answers `409 no_capacity`. */
            in_stock: boolean;
            /** @description Promo tariff — deletion gives no refund. */
            non_refundable: boolean;
            /** @description OS slugs this tariff cannot run. */
            excluded_os?: string[];
            extra_ip_price_monthly: components["schemas"]["Money"];
            max_extra_ips?: number;
        };
        OsImage: {
            /** @description Pass this as `os` when ordering or reinstalling. */
            slug: string;
            name: string;
            /** @enum {string} */
            family: "linux" | "windows" | "other";
            version?: string | null;
            supports_ssh_keys?: boolean;
            min_disk_gb?: number | null;
        };
        Location: {
            id: number;
            code: string;
            name: string;
            country: string;
            city?: string | null;
            in_stock?: boolean;
        };
        Zone: {
            tld: string;
            price_register: components["schemas"]["Money"];
            price_renew: components["schemas"]["Money"];
            price_transfer: components["schemas"]["Money"] | null;
            currency: components["schemas"]["Currency"];
            min_years: number;
            max_years: number;
            privacy_supported: boolean;
            transfer_supported?: boolean;
        };
        Discount: {
            /** @enum {string} */
            type: "group" | "loyalty" | "volume" | "period" | "promo";
            percent: number;
            amount: components["schemas"]["Money"];
        };
        Quote: {
            tariff_id: number;
            billing_cycle: components["schemas"]["BillingCycle"];
            months?: number | null;
            hours?: number | null;
            base_amount: components["schemas"]["Money"];
            discounts: components["schemas"]["Discount"][];
            total: components["schemas"]["Money"];
            currency: components["schemas"]["Currency"];
            promo?: {
                code: string;
                valid: boolean;
                message?: string | null;
            } | null;
            balance: components["schemas"]["Money"];
            balance_sufficient: boolean;
            shortfall: components["schemas"]["Money"];
        };
        /**
         * @description `active` — running or at least not stopped by billing; `stopped` —
         *     powered off by the client; `suspended` — switched off for
         *     non-payment or by staff; `pending_cancel` — a cancel request at end
         *     of period is approved; `cancelled` — deleted by the client or by a
         *     cancel request; `terminated` — removed after prolonged non-payment.
         * @enum {string}
         */
        ServerStatus: "active" | "stopped" | "suspended" | "pending_cancel" | "cancelled" | "terminated";
        ServerFlags: {
            /** @description Locked by staff; every mutation answers `403 server_blocked`. */
            blocked: boolean;
            suspended: boolean;
            /** @description `next_due_at` is in the past. */
            expired: boolean;
            pending_cancel: boolean;
            /** @description Grace protection after migration — no suspension before this date. */
            protected_until: components["schemas"]["NullableTimestamp"];
            /** @description Free test server; deletion refunds nothing. */
            is_test: boolean;
            /** @description Imported service with a synthetic id (`>= 2000000000`) and no panel record. */
            synthetic: boolean;
        };
        ServerBilling: {
            cycle: components["schemas"]["BillingCycle"];
            /** @description Length of one billing period for monthly servers (1 */
            months: number;
            price_monthly: components["schemas"]["Money"];
            /** @description What one renewal costs now (tariff + extra IPs − discounts). */
            recurring_amount: components["schemas"]["Money"];
            currency: components["schemas"]["Currency"];
            next_due_at: components["schemas"]["NullableTimestamp"];
            auto_renew: boolean;
            promo_code: string | null;
            extra_ips: number;
        };
        ServerRef: {
            /** @description `null` for a service whose tariff row is gone. */
            id: number | null;
            name: string;
        };
        /** @description Resources of the tariff behind this server. Any field is `null` when the tariff row no longer carries it. */
        ServerResources: {
            vcpu: number | null;
            ram_mb: number | null;
            /** @description Disk in MiB (not GiB */
            disk_mb: number | null;
        };
        /** @description Never contains passwords; `root_password` appears only in `ServerCreated`. */
        Server: {
            /**
             * Format: int64
             * @description Panel id (`solusvm_id`); sandbox servers are `>= 9000000000`.
             */
            id: number;
            name: string;
            /** @description Falls back to `name` when the panel has no separate host name. */
            hostname: string;
            status: components["schemas"]["ServerStatus"];
            tariff: components["schemas"]["ServerRef"];
            /** @description Location name */
            location: string | null;
            /** @description OS `slug` as ordered, e.g. `ubuntu-24.04`. */
            os: string | null;
            /** @description First assigned address. */
            primary_ip: string | null;
            /** @description All assigned addresses, v4 and v6. */
            ips: string[];
            resources: components["schemas"]["ServerResources"];
            flags: components["schemas"]["ServerFlags"];
            billing: components["schemas"]["ServerBilling"];
            /** @description Client note (`vds_tables.client_notes`); empty string when unset. */
            notes: string;
            /** @description `null` for services imported from WHMCS without a creation date. */
            created_at: components["schemas"]["NullableTimestamp"];
        };
        ServerLiveStatus: {
            /** Format: int64 */
            server_id: number;
            /** @enum {string} */
            power: "running" | "stopped" | "unknown";
            cpu_percent?: number | null;
            memory?: {
                used_mb: number;
                total_mb: number;
            } | null;
            disk?: {
                used_gb: number;
                total_gb: number;
            } | null;
            uptime_seconds?: number | null;
            checked_at: components["schemas"]["Timestamp"];
        };
        RefundBreakdownItem: {
            /** Format: int64 */
            invoice_id: number;
            /** @description What the invoice paid for this window. */
            amount: components["schemas"]["Money"];
            period_start: components["schemas"]["NullableTimestamp"];
            period_end: components["schemas"]["NullableTimestamp"];
            /** @description Whole days left in this window; a display value */
            days_left: number;
            refund: components["schemas"]["Money"];
            paid_at: components["schemas"]["NullableTimestamp"];
        };
        /**
         * @description Prorated over the unused part of the paid period, based on the
         *     invoices that were really paid (not on the tariff price). Zero with
         *     `excluded_reason` when a rule excludes the server. Referral bonuses
         *     paid out for those invoices are deducted, so always
         *     `amount == gross_amount − referral_adjustment`.
         *
         *     `breakdown` is the per-invoice derivation and it is filled **only for
         *     monthly services**, where `Σ breakdown[].refund == gross_amount`. An
         *     hourly server is refunded from the remaining prepaid hours rather than
         *     from invoice windows, so its `breakdown` is empty (`[]`) while
         *     `gross_amount` is positive — reconcile it against `note`, which spells
         *     out hours × rate. Do not treat an empty `breakdown` next to a positive
         *     `gross_amount` as a discrepancy.
         */
        RefundQuote: {
            /** Format: int64 */
            server_id: number;
            /** @description What would actually be credited. */
            amount: components["schemas"]["Money"];
            currency: components["schemas"]["Currency"];
            /** @description `excluded_reason` is null and `amount` > 0. */
            refundable: boolean;
            /**
             * @description Why the refund is zero (or cut):
             *
             *     * `promo_tariff` — non-refundable (promo) tariff;
             *     * `blocked` — the server was blocked by staff;
             *     * `service_state` — already cancelled or terminated;
             *     * `unpaid` — suspended and the paid period is over;
             *     * `no_payment_record` — no paid invoices behind the service
             *       (test servers, staff gifts, sandbox servers);
             *     * `hourly` — hourly server with no prepaid hours left;
             *     * `consumed` — invoices exist but the paid period is used up (or
             *       eaten by `referral_adjustment`);
             *     * `currency_mismatch` — the invoices are not in USD, support
             *       computes the refund by hand;
             *     * `unavailable` — only inside `ServerDetail.refund_quote`: the
             *       computation failed and the card must not claim a zero refund.
             * @enum {string|null}
             */
            excluded_reason: null | "promo_tariff" | "blocked" | "service_state" | "unpaid" | "no_payment_record" | "hourly" | "consumed" | "currency_mismatch" | "unavailable";
            /** @description Days of the paid period left */
            days_left: number;
            /** @description Refund over the invoices before the referral adjustment. */
            gross_amount: components["schemas"]["Money"];
            referral_adjustment: components["schemas"]["Money"];
            /** @description Human-readable explanation of the computation. */
            note: string;
            breakdown: components["schemas"]["RefundBreakdownItem"][];
            computed_at: components["schemas"]["Timestamp"];
        };
        ServerDetail: components["schemas"]["Server"] & {
            refund_quote: components["schemas"]["RefundQuote"];
            /** @description Only with `?include=live`. */
            live?: components["schemas"]["ServerLiveStatus"] | null;
        };
        ServerPage: {
            data: components["schemas"]["Server"][];
            next_cursor: string | null;
        };
        /**
         * @description Give `months` (monthly cycle) or `hours` (hourly cycle, when the
         *     tariff allows it), not both; sending both is `400`.
         */
        ServerCreate: {
            tariff_id: number;
            /** @description `slug` from `GET /catalog/os`. */
            os: string;
            /** @description Host name (`A-Za-z0-9._-`). Generated when omitted. */
            name?: string;
            /**
             * @deprecated
             * @description Alias of `name`, accepted for compatibility with the cabinet. Use `name`.
             */
            server_name?: string;
            /**
             * @description Root password: 8–64 characters, at least one letter and one digit,
             *     only latin letters, digits and `!@#$%^&*-` (the panel accepts
             *     nothing else). Generated by VDSok and returned once when omitted.
             */
            password?: string;
            /**
             * @default 1
             * @enum {integer}
             */
            months?: 1 | 3 | 6 | 12;
            /**
             * @description Ordering an hourly server prepays exactly one hour; buy more with `POST /servers/{server_id}/renew`.
             * @constant
             */
            hours?: 1;
            billing_cycle?: components["schemas"]["BillingCycle"];
            promo_code?: string;
            ssh_key_ids?: number[];
            /** @description Answers to the order form fields configured by VDSok, if any. At most 30 keys; the values are stored as text. */
            custom_fields?: {
                [key: string]: string;
            };
        };
        ServerCreated: {
            server: components["schemas"]["Server"];
            /** @description Shown once; blanked in idempotent replays. */
            root_password: string;
            /**
             * Format: int64
             * @description `null` for a sandbox order: nothing was written.
             */
            invoice_id: number | null;
            charged: components["schemas"]["Money"];
            currency: components["schemas"]["Currency"];
            balance_after: components["schemas"]["Money"];
            /** @description Relative API path of the order; `null` in the sandbox. */
            order_url: string | null;
        };
        ServerUpdate: {
            auto_renew?: boolean;
            name?: string;
            notes?: string | null;
        };
        DeleteResult: {
            /** Format: int64 */
            server_id: number;
            /** @constant */
            status: "cancelled";
            /** @description What was credited to the balance; `refund_quote.amount` is the same number. */
            refund: components["schemas"]["Money"];
            currency: components["schemas"]["Currency"];
            balance_after: components["schemas"]["Money"];
            /** @description How the refund was computed. A pending cancel request is withdrawn silently */
            refund_quote: components["schemas"]["RefundQuote"];
        };
        RenewRequest: {
            /** @enum {integer} */
            months?: 1 | 3 | 6 | 12;
            /** @description 1..2160 (90 days) */
            hours?: number;
        } & (unknown | unknown);
        RenewResult: {
            server: components["schemas"]["Server"];
            /**
             * Format: int64
             * @description `null` in the sandbox and when the invoice could not be looked up; the renewal itself still happened.
             */
            invoice_id: number | null;
            charged: components["schemas"]["Money"];
            currency: components["schemas"]["Currency"];
            balance_after: components["schemas"]["Money"];
            next_due_at: components["schemas"]["Timestamp"];
            /** @description Present when renewed with `months`. */
            months?: number;
            /** @description Present when renewed with `hours`. */
            hours?: number;
        };
        PowerRequest: {
            /** @enum {string} */
            action: "start" | "stop" | "restart";
        };
        ActionResult: {
            /** Format: int64 */
            server_id: number;
            action: string;
            /** @enum {string} */
            status: "accepted" | "done";
            message?: string | null;
        };
        ReinstallRequest: {
            os: string;
            /** @description Same rules as `ServerCreate.password`; generated and returned once when omitted. */
            password?: string;
            ssh_key_ids?: number[];
        };
        ReinstallResult: {
            /** Format: int64 */
            server_id: number;
            /** @constant */
            status: "reinstalling";
            os: string;
            /** @description Only when generated by VDSok. */
            root_password: string | null;
        };
        ResetPasswordResult: {
            /** Format: int64 */
            server_id: number;
            password: string;
        };
        /**
         * @description Exactly what `services/vds_orders.get_order_status` can report:
         *
         *     * `provisioning` — charged, the VM is being created (poll);
         *     * `invoice_created` — the invoice is not paid yet (gateway payment);
         *     * `processing` — paid, the server has not appeared yet;
         *     * `active` — done, `server_id` is filled in;
         *     * `cancelled` — the invoice was cancelled and the charge refunded.
         *
         *     Terminal states are `active` and `cancelled`.
         * @enum {string}
         */
        OrderStatus: "provisioning" | "invoice_created" | "processing" | "active" | "cancelled";
        Order: {
            /** Format: int64 */
            invoice_id: number;
            status: components["schemas"]["OrderStatus"];
            /** Format: int64 */
            server_id: number | null;
            /** @description Only in orders returned by `GET /orders` and `GET /orders/{invoice_id}`. */
            server_name?: string | null;
            /** @description Only in orders returned by `GET /orders` and `GET /orders/{invoice_id}`. */
            tariff_name?: string | null;
            /** @description From the invoice metadata; `null` when it was not recorded. */
            tariff_id: number | null;
            amount: components["schemas"]["Money"];
            currency: components["schemas"]["Currency"];
            created_at: components["schemas"]["NullableTimestamp"];
            /** @description Time of payment */
            updated_at: components["schemas"]["NullableTimestamp"];
            /** @description Relative API path to poll. */
            order_url: string;
            /** @description Reserved; always `null` today — a failed order is refunded and reported as `cancelled`. */
            failure_reason: string | null;
        };
        OrderPage: {
            data: components["schemas"]["Order"][];
            next_cursor: string | null;
        };
        Ip: {
            /** Format: int64 */
            id: number;
            address: string;
            /** @enum {integer} */
            version: 4 | 6;
            primary: boolean;
            ptr: string | null;
            gateway?: string | null;
            netmask?: string | null;
            /** @description `0.00` for the primary address. */
            price_monthly: components["schemas"]["Money"];
            added_at: components["schemas"]["Timestamp"];
        };
        IpQuote: {
            /** Format: int64 */
            server_id: number;
            price_monthly: components["schemas"]["Money"];
            /** @description Charged immediately for the rest of the current period. */
            prorated_now: components["schemas"]["Money"];
            currency: components["schemas"]["Currency"];
            next_due_at: components["schemas"]["NullableTimestamp"];
            extra_ips: number;
            max_extra_ips: number;
            balance_sufficient: boolean;
        };
        /** @description The body may be omitted entirely; `count` exists only so that `count != 1` is refused explicitly — the panel hands out addresses one at a time. */
        IpAddRequest: {
            /**
             * @default 1
             * @constant
             */
            count?: 1;
        };
        /** @description The address itself is not here — the panel assigns it asynchronously; read it from `GET /servers/{server_id}/ips`. */
        IpAdded: {
            /** @constant */
            success: true;
            /** Format: int64 */
            server_id: number;
            /** @description Prorated price for the rest of the paid period. */
            charged: components["schemas"]["Money"];
            currency: components["schemas"]["Currency"];
            /** @description Days of the paid period the charge covers. */
            days: number;
        };
        IpDeleted: {
            /** @constant */
            status: "deleted";
            /** Format: int64 */
            server_id: number;
            /** Format: int64 */
            ip_id: number;
        };
        PtrUpdate: {
            /**
             * @description Hostname for the reverse record (letters, digits, dots, hyphens;
             *     at least two labels). `""`, `null` or an empty body removes the
             *     record.
             */
            domain?: string | null;
        };
        /** @description The stored reverse record. Read the rest of the address from `GET /servers/{server_id}/ips`. */
        PtrRecord: {
            /**
             * Format: int64
             * @description Id of the address.
             */
            id: number;
            /** @description `null` when the record was removed. */
            ptr: string | null;
        };
        SshKey: {
            /** Format: int64 */
            id: number;
            name: string;
            /** @description SHA256:… of the public key. */
            fingerprint: string;
            /**
             * @description Key algorithm as it appears in the first field of the OpenSSH line; the same set the cabinet accepts.
             * @enum {string}
             */
            type: "ssh-ed25519" | "ssh-rsa" | "ssh-dss" | "ecdsa-sha2-nistp256" | "ecdsa-sha2-nistp384" | "ecdsa-sha2-nistp521";
            public_key: string;
            created_at: components["schemas"]["Timestamp"];
        };
        SshKeyCreate: {
            name: string;
            /** @description One line in OpenSSH format. */
            public_key: string;
        };
        /**
         * @description Derived server-side from the `domains` row — there is no single status
         *     column, so the exact rule is spelled out here and must stay in sync
         *     with the serializer:
         *
         *     * `cancelled` — `domains.blocked = 1` (staff removed the service);
         *     * `transfer_pending` — `registrar_status` reported by the registrar is
         *       a pending-transfer state and the domain is not active yet;
         *     * `pending` — registered here but the registrar has not confirmed it
         *       yet (`registrar_synced_at IS NULL` and no `expires_at`), or
         *       `registrar_status` is a pending-registration state;
         *     * `expired` — the expiry moment is in the past (`expires_at` from the
         *       registrar, otherwise `last_billed + period` years) or
         *       `domains.suspended = 1`;
         *     * `active` — everything else.
         *
         *     `blocked` is exposed separately as its own boolean, because a blocked
         *     domain still needs its other fields to round-trip.
         * @enum {string}
         */
        DomainStatus: "pending" | "active" | "expired" | "transfer_pending" | "cancelled";
        Domain: {
            /** Format: int64 */
            id: number;
            name: string;
            tld: string;
            status: components["schemas"]["DomainStatus"];
            /** @description When the domain was taken on here (`domains.created_at`); for domains imported from WHMCS this is the import-era value. */
            registered_at: components["schemas"]["NullableTimestamp"];
            /** @description Registrar expiry (`domains.expires_at`); until the first registrar sync it is computed as `last_billed + period` years, the same way the cabinet does it. */
            expires_at: components["schemas"]["NullableTimestamp"];
            auto_renew: boolean;
            /** @description WHOIS privacy enabled at the registrar. */
            privacy: boolean;
            /** @description Exactly two — `domains.ns1` and `domains.ns2`. VDSok stores no further slots. */
            nameservers: string[];
            /** @description Transfer lock at the registrar (`domains.registrar_locked`; `false` until the first registrar sync). */
            locked: boolean;
            /** @description Locked by staff; mutations answer `403 domain_blocked`. */
            blocked: boolean;
            price_renew?: components["schemas"]["Money"];
            currency?: components["schemas"]["Currency"];
            created_at: components["schemas"]["Timestamp"];
        };
        DomainPage: {
            data: components["schemas"]["Domain"][];
            next_cursor: string | null;
        };
        AvailabilityResult: {
            name: string;
            available: boolean;
            /**
             * @description Why it cannot be registered here.
             * @enum {string|null}
             */
            reason: null | "taken" | "premium" | "reserved" | "invalid" | "unsupported_tld";
            premium?: boolean;
            price_register: components["schemas"]["Money"] | null;
            price_renew: components["schemas"]["Money"] | null;
            currency: components["schemas"]["Currency"];
            min_years?: number;
        };
        /** @description Registrant contact data is taken from the account profile (the same as in the cabinet). */
        DomainRegister: {
            name: string;
            /** @default 1 */
            years?: number;
            /** @description Exactly two; defaults to the VDSok nameservers. */
            nameservers?: string[];
            /** @default false */
            privacy?: boolean;
            promo_code?: string;
        };
        /**
         * @description `201` when the registrar confirmed, `202 pending` when it did not
         *     answer in time — the charge stays and staff reconcile it by hand.
         */
        DomainOrderResult: {
            /** @enum {string} */
            status: "registered" | "pending";
            /** @description The domain name */
            domain: string;
            /**
             * Format: int64
             * @description `null` while the domain row does not exist yet (pending, sandbox).
             */
            domain_id: number | null;
            zone: string | null;
            years: number;
            charged: components["schemas"]["Money"];
            /** @description Promo-code discount already included in `charged`. */
            discount: components["schemas"]["Money"];
            /** @description The code that was actually applied. */
            promo_code: string | null;
            currency: components["schemas"]["Currency"];
            expires_at: components["schemas"]["NullableTimestamp"];
            message: string | null;
        };
        DomainRenew: {
            years: number;
        };
        DomainRenewResult: {
            /**
             * @description `pending` comes with `202`; `pending_sync` is a success (`200`) whose new expiry date is picked up by the daily sync.
             * @enum {string}
             */
            status: "renewed" | "pending_sync" | "pending";
            /** @description The domain name */
            domain: string | null;
            /** Format: int64 */
            domain_id: number;
            /** @description Years added by this call. */
            years: number;
            /** @description Paid period after the renewal; early renewal stacks. */
            total_years: number | null;
            charged: components["schemas"]["Money"];
            currency: components["schemas"]["Currency"];
            balance_after: components["schemas"]["Money"];
            expires_at: components["schemas"]["NullableTimestamp"];
            message: string | null;
        };
        /** @description Always `202`; the registrar takes 5–7 days. Poll `GET /domains/{domain_id}` until `status` turns `active`. */
        DomainTransferResult: {
            /**
             * @description `pending` — the registrar did not answer in time and the charge was kept on purpose.
             * @enum {string}
             */
            status: "pending_transfer" | "pending";
            domain: string;
            /** Format: int64 */
            domain_id: number | null;
            charged: components["schemas"]["Money"];
            currency: components["schemas"]["Currency"];
            message: string | null;
        };
        NameserversUpdate: {
            /** @description Exactly two; both are sent to the registrar and stored as `ns1`/`ns2`. */
            nameservers: string[];
        };
        DomainUpdate: {
            auto_renew?: boolean;
            privacy?: boolean;
        };
        DomainTransfer: {
            name: string;
            /** @description EPP / transfer code from the current registrar. */
            auth_code: string;
            /** @description Exactly two; defaults to the VDSok nameservers. */
            nameservers?: string[];
        };
        /** @description The secret is never returned; `hint` is prefix + last 4 characters. */
        ApiKey: {
            /** Format: int64 */
            id: number;
            name: string;
            hint: string;
            /** @enum {string} */
            mode: "live" | "test";
            scopes: components["schemas"]["Scope"][];
            /** @description IPs or CIDRs; empty = any. */
            ip_allowlist: string[];
            rate_per_minute: number | null;
            rate_expensive_per_minute: number | null;
            not_before: components["schemas"]["NullableTimestamp"];
            expires_at: components["schemas"]["NullableTimestamp"];
            /** @description `api_keys.active` and not revoked. */
            active: boolean;
            revoked_at: components["schemas"]["NullableTimestamp"];
            revoked_reason: string | null;
            last_used_at: components["schemas"]["NullableTimestamp"];
            /** @description Refreshed at most once a minute per key. */
            last_used_ip: string | null;
            created_at: components["schemas"]["Timestamp"];
        };
        ApiKeyPage: {
            data: components["schemas"]["ApiKey"][];
            /** @description Always `null` today — the whole list fits in one response; the field exists so a future cursor does not break clients. */
            next_cursor: string | null;
        };
        ApiKeyRevoked: {
            /** @constant */
            status: "revoked";
            key: components["schemas"]["ApiKey"];
        };
        /** @enum {string} */
        WebhookEventType: "server.created" | "server.suspended" | "server.unsuspended" | "server.terminated" | "server.reinstalled" | "invoice.created" | "invoice.paid" | "invoice.overdue" | "balance.low" | "domain.registered" | "domain.expiring" | "domain.renewed" | "key.created" | "key.revoked" | "ping";
        WebhookEventSelector: components["schemas"]["WebhookEventType"] | "*";
        WebhookEventDescriptor: {
            type: components["schemas"]["WebhookEventType"];
            /** @description Short human label (Russian) */
            description: string;
        };
        WebhookSubscription: {
            /** Format: int64 */
            id: number;
            /** Format: uri */
            url: string;
            events: components["schemas"]["WebhookEventSelector"][];
            description: string;
            active: boolean;
            failures_in_row: number;
            disabled_at: components["schemas"]["NullableTimestamp"];
            /** @description Free text, e.g. `20 failures in a row: …`. */
            disabled_reason: string | null;
            last_delivery_at: components["schemas"]["NullableTimestamp"];
            /** @description HTTP status of the last attempt. */
            last_status: number | null;
            created_at: components["schemas"]["Timestamp"];
        };
        WebhookSubscriptionPage: {
            data: components["schemas"]["WebhookSubscription"][];
            /** @description Always `null` today — the whole list fits in one response; the field exists so a future cursor does not break clients. */
            next_cursor: string | null;
        };
        WebhookSubscriptionWithSecret: components["schemas"]["WebhookSubscription"] & {
            /** @description `whsec_…`, shown once. */
            secret: string;
        };
        /** @description Result of a secret rotation — only the id and the new secret. */
        WebhookSecret: {
            /** Format: int64 */
            id: number;
            /** @description `whsec_…`, shown once. */
            secret: string;
        };
        WebhookDeleted: {
            /** @constant */
            status: "deleted";
            /** Format: int64 */
            id: number;
        };
        WebhookSubscriptionCreate: {
            /**
             * Format: uri
             * @description `https://` on a public address; no redirects are followed.
             */
            url: string;
            events: components["schemas"]["WebhookEventSelector"][];
            /** @default  */
            description?: string;
        };
        WebhookSubscriptionUpdate: {
            /** Format: uri */
            url?: string;
            events?: components["schemas"]["WebhookEventSelector"][];
            description?: string;
            /** @description Set `true` to re-enable after auto-disable; resets `failures_in_row`, `disabled_at` and `disabled_reason`. */
            active?: boolean;
        };
        WebhookDelivery: {
            /** Format: int64 */
            id: number;
            /** Format: int64 */
            subscription_id: number;
            event_id: string;
            event_type: components["schemas"]["WebhookEventType"];
            attempts: number;
            next_attempt_at: components["schemas"]["Timestamp"];
            delivered_at: components["schemas"]["NullableTimestamp"];
            /** @description No further attempts will be made; re-queue it with `POST /webhooks/deliveries/{delivery_id}/redeliver`. */
            dead: boolean;
            last_status: number | null;
            last_error: string | null;
            /** @description First 1000 bytes of the receiver's body. */
            last_response: string | null;
            created_at: components["schemas"]["Timestamp"];
        };
        WebhookDeliveryWithPayload: components["schemas"]["WebhookDelivery"] & {
            /** @description The exact envelope that was sent. */
            payload: components["schemas"]["WebhookEvent"];
        };
        WebhookDeliveryPage: {
            data: components["schemas"]["WebhookDelivery"][];
            next_cursor: string | null;
        };
        WebhookTestResult: {
            delivery: components["schemas"]["WebhookDelivery"];
            /** @description The receiver answered 2xx. */
            ok: boolean;
            /** @description HTTP status returned by the receiver; null when the request never completed. */
            status: number | null;
            /** @description Measured around the inline delivery */
            latency_ms: number;
            /** @description Error text when `ok` is false */
            detail: string | null;
        };
        WebhookRedelivery: {
            /** @constant */
            status: "queued";
            delivery: components["schemas"]["WebhookDelivery"];
        };
        /**
         * @description Envelope of every delivery. Serialized once when the event is queued;
         *     retries and redeliveries send the identical bytes, so `id`,
         *     `created_at` and the signature input never change. `data.object` is
         *     the full snapshot in the same shape the REST API returns;
         *     `data.previous` holds the fields that changed (e.g. `{"status":
         *     "active"}` on `server.suspended`) and is absent when the event has no
         *     prior state; `resource` is the API path of the object.
         */
        WebhookEvent: {
            id: string;
            type: components["schemas"]["WebhookEventType"];
            created_at: components["schemas"]["Timestamp"];
            /** @description Always true — webhooks are live-only. */
            livemode: boolean;
            /** Format: int64 */
            account_id: number;
            /** @constant */
            api_version: "1";
            data: {
                object: {
                    [key: string]: unknown;
                };
                previous?: {
                    [key: string]: unknown;
                };
            };
            /** @description Relative API path, e.g. `/api/v1/servers/2001`. */
            resource: string | null;
        };
        WebhookEventServer: components["schemas"]["WebhookEvent"] & {
            /** @enum {string} */
            type?: "server.created" | "server.suspended" | "server.unsuspended" | "server.terminated" | "server.reinstalled";
            data?: {
                object?: components["schemas"]["Server"];
            };
        };
        WebhookEventInvoice: components["schemas"]["WebhookEvent"] & {
            /** @enum {string} */
            type?: "invoice.created" | "invoice.paid" | "invoice.overdue";
            data?: {
                object?: components["schemas"]["Invoice"];
            };
        };
        WebhookEventBalance: components["schemas"]["WebhookEvent"] & {
            /** @constant */
            type?: "balance.low";
            data?: {
                object?: components["schemas"]["Balance"];
            };
            /** @constant */
            resource?: "/api/v1/balance";
        };
        WebhookEventDomain: components["schemas"]["WebhookEvent"] & {
            /** @enum {string} */
            type?: "domain.registered" | "domain.expiring" | "domain.renewed";
            data?: {
                object?: components["schemas"]["Domain"];
            };
        };
        WebhookEventKey: components["schemas"]["WebhookEvent"] & {
            /** @enum {string} */
            type?: "key.created" | "key.revoked";
            data?: {
                object?: components["schemas"]["ApiKey"];
            };
        };
        WebhookEventPing: components["schemas"]["WebhookEvent"] & {
            /** @constant */
            type?: "ping";
            data?: {
                object?: {
                    /** Format: int64 */
                    subscription_id: number;
                    message: string;
                };
            };
            resource?: null;
        };
    };
    responses: {
        /** @description Any 2xx marks the delivery as done; the body is ignored. */
        WebhookAck: {
            headers: {
                [name: string]: unknown;
            };
            content?: never;
        };
        /**
         * @description Malformed request. Codes: `invalid_request`, `validation_error`
         *     (`details.fields`), `invalid_cursor`, `idempotency_key_required`,
         *     `os_not_allowed`, `invalid_period`, `invalid_action`,
         *     `upstream_rejected` (registrar or panel refused the parameters).
         */
        BadRequest: {
            headers: {
                "X-Request-ID": components["headers"]["X-Request-ID"];
                "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                "X-Sandbox": components["headers"]["X-Sandbox"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
        /** @description Codes: `invalid_token`, `key_expired`, `key_not_yet_valid`. */
        Unauthorized: {
            headers: {
                "X-Request-ID": components["headers"]["X-Request-ID"];
                "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                "X-Sandbox": components["headers"]["X-Sandbox"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
        /** @description `insufficient_funds`. Nothing was charged or created; `details` has `required`, `balance`, `shortfall`, `currency`. */
        PaymentRequired: {
            headers: {
                "X-Request-ID": components["headers"]["X-Request-ID"];
                "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                "X-Sandbox": components["headers"]["X-Sandbox"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
        /**
         * @description Codes: `insufficient_scope` (`details.required`), `ip_not_allowed`,
         *     `account_suspended`, `api_disabled_for_account`,
         *     `sandbox_not_supported` (test key on a `forbidden` operation),
         *     `sandbox_disabled` (test keys switched off globally), `server_blocked`,
         *     `domain_blocked`.
         */
        Forbidden: {
            headers: {
                "X-Request-ID": components["headers"]["X-Request-ID"];
                "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                "X-Sandbox": components["headers"]["X-Sandbox"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
        /** @description `not_found` — no such object on this account (also for objects owned by someone else). */
        NotFound: {
            headers: {
                "X-Request-ID": components["headers"]["X-Request-ID"];
                "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                "X-Sandbox": components["headers"]["X-Sandbox"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
        /**
         * @description State conflict. Codes: `conflict`, `idempotency_conflict` (same key,
         *     different request), `idempotency_in_progress` (retry after
         *     `Retry-After`), `operation_in_progress` (another order/delete/IP
         *     change on the same object is running), `service_state`, `no_capacity`,
         *     `tariff_unavailable`, `ip_limit_reached`, `domain_taken`,
         *     `domain_exists`, `cancel_pending`.
         */
        Conflict: {
            headers: {
                "X-Request-ID": components["headers"]["X-Request-ID"];
                "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                "X-Sandbox": components["headers"]["X-Sandbox"];
                "Retry-After": components["headers"]["Retry-After"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
        /** @description `payload_too_large` — request body over 64 KB. */
        PayloadTooLarge: {
            headers: {
                "X-Request-ID": components["headers"]["X-Request-ID"];
                "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                "X-Sandbox": components["headers"]["X-Sandbox"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
        /** @description `unsupported_media_type` — body is not `application/json`. */
        UnsupportedMediaType: {
            headers: {
                "X-Request-ID": components["headers"]["X-Request-ID"];
                "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                "X-Sandbox": components["headers"]["X-Sandbox"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
        /** @description `rate_limited` — the per-key bucket (normal or expensive) or the anonymous per-IP bucket is exhausted. */
        RateLimited: {
            headers: {
                "X-Request-ID": components["headers"]["X-Request-ID"];
                "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                "X-Sandbox": components["headers"]["X-Sandbox"];
                "Retry-After": components["headers"]["Retry-After"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
        /** @description `server_error` — unexpected failure; the request id is logged. */
        ServerError: {
            headers: {
                "X-Request-ID": components["headers"]["X-Request-ID"];
                "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                "X-Sandbox": components["headers"]["X-Sandbox"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
        /** @description `upstream_error` — the panel, registrar or gateway answered with an error. For money operations nothing was charged unless the response says otherwise. */
        BadGateway: {
            headers: {
                "X-Request-ID": components["headers"]["X-Request-ID"];
                "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                "X-Sandbox": components["headers"]["X-Sandbox"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
        /**
         * @description Codes: `api_disabled` (switched off by staff), `upstream_unavailable`,
         *     `temporarily_unavailable` (e.g. the discount could not be computed —
         *     we refuse rather than silently charge full price). Retry after
         *     `Retry-After`.
         */
        ServiceUnavailable: {
            headers: {
                "X-Request-ID": components["headers"]["X-Request-ID"];
                "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                "X-Sandbox": components["headers"]["X-Sandbox"];
                "Retry-After": components["headers"]["Retry-After"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
        /** @description `upstream_timeout` — the panel or registrar did not answer in time. For `POST /servers` the charge is kept and the order continues (see `202`). */
        GatewayTimeout: {
            headers: {
                "X-Request-ID": components["headers"]["X-Request-ID"];
                "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                "X-Sandbox": components["headers"]["X-Sandbox"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
    };
    parameters: {
        /**
         * @description Optional trace id of your own. VDSok keeps only the characters
         *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
         *     sanitized value becomes the id of the request — it comes back in the
         *     `X-Request-ID` response header, appears in the error envelope as
         *     `error.request_id` and is stored in the request log. If nothing is
         *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
         *     malformed header is never an error. Reuse across different requests
         *     is allowed but makes support harder; the SDKs send a fresh id per
         *     attempt.
         */
        RequestId: string;
        /** @description Opaque `next_cursor` from the previous page. */
        Cursor: string;
        Limit: number;
        /** @description Unique per logical request, 16..128 chars (a UUID is fine). Remembered for 7 days. */
        IdempotencyKey: string;
        /** @description Panel id of the VM (`solusvm_id`). */
        ServerId: number;
        InvoiceId: number;
        TariffId: number;
        IpId: number;
        SshKeyId: number;
        DomainId: number;
        ApiKeyId: number;
        WebhookId: number;
        DeliveryId: number;
    };
    requestBodies: {
        ServerEvent: {
            content: {
                "application/json": components["schemas"]["WebhookEventServer"];
            };
        };
        InvoiceEvent: {
            content: {
                "application/json": components["schemas"]["WebhookEventInvoice"];
            };
        };
        BalanceEvent: {
            content: {
                "application/json": components["schemas"]["WebhookEventBalance"];
            };
        };
        DomainEvent: {
            content: {
                "application/json": components["schemas"]["WebhookEventDomain"];
            };
        };
        KeyEvent: {
            content: {
                "application/json": components["schemas"]["WebhookEventKey"];
            };
        };
        PingEvent: {
            content: {
                "application/json": components["schemas"]["WebhookEventPing"];
            };
        };
    };
    headers: {
        /**
         * @description Id of this request; quote it when contacting support. Echoes the
         *     sanitized `X-Request-ID` you sent when anything survived sanitizing,
         *     otherwise a server-generated UUIDv4.
         */
        "X-Request-ID": string;
        /** @description `no-store` on every endpoint except `GET /openapi.json`, which is `public, max-age=300`. */
        "Cache-Control": string;
        /** @description Requests allowed per minute for this key in the bucket the call hit (normal or expensive). */
        "X-RateLimit-Limit": number;
        /** @description Requests left in the current 60-second window. */
        "X-RateLimit-Remaining": number;
        /** @description Unix time (seconds) when the window resets. */
        "X-RateLimit-Reset": number;
        /** @description Present with value `true` on every response to a test key; absent for live keys. */
        "X-Sandbox": "true";
        /** @description Seconds to wait before retrying. */
        "Retry-After": number;
    };
    pathItems: never;
};
export type $defs = Record<string, never>;
export interface operations {
    get_health: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Service state */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Health"];
                };
            };
        };
    };
    get_openapi: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OpenAPI 3.1 document */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    "Cache-Control": components["headers"]["Cache-Control"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        [key: string]: unknown;
                    };
                };
            };
        };
    };
    get_me: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Calling key and account */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Me"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    get_account: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Account */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Account"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    get_balance: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Balance */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Balance"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    list_transactions: {
        parameters: {
            query?: {
                /** @description Opaque `next_cursor` from the previous page. */
                cursor?: components["parameters"]["Cursor"];
                limit?: components["parameters"]["Limit"];
                direction?: "credit" | "debit";
                /** @description Only transactions created at or after this moment. */
                since?: components["schemas"]["Timestamp"];
                until?: components["schemas"]["Timestamp"];
            };
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Page of transactions */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TransactionPage"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    get_topup_info: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Limits and gateways */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TopupInfo"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    create_topup: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
                /** @description Unique per logical request, 16..128 chars (a UUID is fine). Remembered for 7 days. */
                "Idempotency-Key": components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["TopupRequest"];
            };
        };
        responses: {
            /** @description Invoice created */
            201: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TopupResult"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    list_invoices: {
        parameters: {
            query?: {
                /** @description Opaque `next_cursor` from the previous page. */
                cursor?: components["parameters"]["Cursor"];
                limit?: components["parameters"]["Limit"];
                status?: components["schemas"]["InvoiceStatus"];
                type?: components["schemas"]["InvoiceType"];
            };
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Page of invoices */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InvoicePage"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    get_invoice: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                invoice_id: components["parameters"]["InvoiceId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Invoice */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Invoice"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    get_invoice_pdf: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                invoice_id: components["parameters"]["InvoiceId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description PDF document */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    "Content-Disposition"?: string;
                    [name: string]: unknown;
                };
                content: {
                    "application/pdf": string;
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    pay_invoice: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
                /** @description Unique per logical request, 16..128 chars (a UUID is fine). Remembered for 7 days. */
                "Idempotency-Key": components["parameters"]["IdempotencyKey"];
            };
            path: {
                invoice_id: components["parameters"]["InvoiceId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Paid */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PayInvoiceResult"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["PaymentRequired"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    create_invoice_payment_link: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                invoice_id: components["parameters"]["InvoiceId"];
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["PaymentLinkRequest"];
            };
        };
        responses: {
            /** @description Link */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PaymentLink"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    list_tariffs: {
        parameters: {
            query?: {
                location_id?: number;
            };
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Tariffs */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        data: components["schemas"]["Tariff"][];
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    get_tariff: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                tariff_id: components["parameters"]["TariffId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Tariff */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Tariff"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    list_os_images: {
        parameters: {
            query?: {
                tariff_id?: number;
            };
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Images */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        data: components["schemas"]["OsImage"][];
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    list_locations: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Locations */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        data: components["schemas"]["Location"][];
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    list_zones: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Zones */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        data: components["schemas"]["Zone"][];
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    get_quote: {
        parameters: {
            query: {
                tariff_id: number;
                months?: 1 | 3 | 6 | 12;
                /** @description 1..2160 (90 days) — the same ceiling the cabinet applies to a single hourly prepayment. */
                hours?: number;
                billing_cycle?: components["schemas"]["BillingCycle"];
                promo_code?: string;
            };
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Quote */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Quote"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    list_servers: {
        parameters: {
            query?: {
                /** @description Opaque `next_cursor` from the previous page. */
                cursor?: components["parameters"]["Cursor"];
                limit?: components["parameters"]["Limit"];
                status?: components["schemas"]["ServerStatus"];
                ip?: string;
            };
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Page of servers */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ServerPage"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    create_server: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
                /** @description Unique per logical request, 16..128 chars (a UUID is fine). Remembered for 7 days. */
                "Idempotency-Key": components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ServerCreate"];
            };
        };
        responses: {
            /** @description Server created and running */
            201: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ServerCreated"];
                };
            };
            /** @description Charged, VM creation still in progress */
            202: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Order"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["PaymentRequired"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    get_server: {
        parameters: {
            query?: {
                include?: "live";
            };
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Server */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ServerDetail"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    delete_server: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
                /** @description Unique per logical request, 16..128 chars (a UUID is fine). Remembered for 7 days. */
                "Idempotency-Key": components["parameters"]["IdempotencyKey"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeleteResult"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    update_server: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ServerUpdate"];
            };
        };
        responses: {
            /** @description Updated server */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Server"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    get_server_status: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Live status */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ServerLiveStatus"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    get_server_refund_quote: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Refund quote */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RefundQuote"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    renew_server: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
                /** @description Unique per logical request, 16..128 chars (a UUID is fine). Remembered for 7 days. */
                "Idempotency-Key": components["parameters"]["IdempotencyKey"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RenewRequest"];
            };
        };
        responses: {
            /** @description Renewed */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RenewResult"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["PaymentRequired"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    power_server: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PowerRequest"];
            };
        };
        responses: {
            /** @description Command accepted by the panel */
            202: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ActionResult"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    reinstall_server: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ReinstallRequest"];
            };
        };
        responses: {
            /** @description Reinstall started */
            202: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ReinstallResult"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    reset_server_password: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description New password, shown once */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ResetPasswordResult"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    list_orders: {
        parameters: {
            query?: {
                /** @description Opaque `next_cursor` from the previous page. */
                cursor?: components["parameters"]["Cursor"];
                limit?: components["parameters"]["Limit"];
                /** @description Only `provisioning` is accepted; any other value answers `400 validation_error`. */
                status?: "provisioning";
            };
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Page of orders */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OrderPage"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    get_order: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                invoice_id: components["parameters"]["InvoiceId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Order */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Order"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    list_server_ips: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Addresses */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        data: components["schemas"]["Ip"][];
                    };
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    add_server_ip: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
                /** @description Unique per logical request, 16..128 chars (a UUID is fine). Remembered for 7 days. */
                "Idempotency-Key": components["parameters"]["IdempotencyKey"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["IpAddRequest"];
            };
        };
        responses: {
            /** @description Address bought and charged */
            201: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["IpAdded"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["PaymentRequired"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    get_server_ip_quote: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Quote */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["IpQuote"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    delete_server_ip: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
                ip_id: components["parameters"]["IpId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Released */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["IpDeleted"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    set_server_ip_ptr: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                /** @description Panel id of the VM (`solusvm_id`). */
                server_id: components["parameters"]["ServerId"];
                ip_id: components["parameters"]["IpId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PtrUpdate"];
            };
        };
        responses: {
            /** @description The stored record */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PtrRecord"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    list_ssh_keys: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Keys */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        data: components["schemas"]["SshKey"][];
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    create_ssh_key: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SshKeyCreate"];
            };
        };
        responses: {
            /** @description Stored key */
            201: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SshKey"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    delete_ssh_key: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                key_id: components["parameters"]["SshKeyId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Removed */
            204: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    check_domain_availability: {
        parameters: {
            query: {
                /** @description Full domain name, IDN allowed (sent as punycode or Unicode). */
                name: string;
            };
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Availability and price */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AvailabilityResult"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    list_domains: {
        parameters: {
            query?: {
                /** @description Opaque `next_cursor` from the previous page. */
                cursor?: components["parameters"]["Cursor"];
                limit?: components["parameters"]["Limit"];
                status?: components["schemas"]["DomainStatus"];
            };
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Page of domains */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DomainPage"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    register_domain: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
                /** @description Unique per logical request, 16..128 chars (a UUID is fine). Remembered for 7 days. */
                "Idempotency-Key": components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DomainRegister"];
            };
        };
        responses: {
            /** @description Registered */
            201: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DomainOrderResult"];
                };
            };
            /** @description Accepted by the registrar, pending */
            202: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DomainOrderResult"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["PaymentRequired"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    get_domain: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                domain_id: components["parameters"]["DomainId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Domain */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Domain"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    update_domain: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                domain_id: components["parameters"]["DomainId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DomainUpdate"];
            };
        };
        responses: {
            /** @description Updated domain */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Domain"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    renew_domain: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
                /** @description Unique per logical request, 16..128 chars (a UUID is fine). Remembered for 7 days. */
                "Idempotency-Key": components["parameters"]["IdempotencyKey"];
            };
            path: {
                domain_id: components["parameters"]["DomainId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DomainRenew"];
            };
        };
        responses: {
            /** @description Renewed (`renewed`, or `pending_sync` when only our record lags behind) */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DomainRenewResult"];
                };
            };
            /** @description Charged, the registrar has not answered yet (`pending`) */
            202: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DomainRenewResult"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["PaymentRequired"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    set_domain_nameservers: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                domain_id: components["parameters"]["DomainId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["NameserversUpdate"];
            };
        };
        responses: {
            /** @description Updated domain */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Domain"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    transfer_domain: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
                /** @description Unique per logical request, 16..128 chars (a UUID is fine). Remembered for 7 days. */
                "Idempotency-Key": components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DomainTransfer"];
            };
        };
        responses: {
            /** @description Transfer started */
            202: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DomainTransferResult"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["PaymentRequired"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    list_api_keys: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Keys (never the secret itself) */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiKeyPage"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    get_api_key: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                key_id: components["parameters"]["ApiKeyId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Key */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiKey"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    revoke_api_key: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                key_id: components["parameters"]["ApiKeyId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Revoked */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ApiKeyRevoked"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    list_webhooks: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Subscriptions */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebhookSubscriptionPage"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    create_webhook: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["WebhookSubscriptionCreate"];
            };
        };
        responses: {
            /** @description Subscription with its secret */
            201: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebhookSubscriptionWithSecret"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    list_webhook_events: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Event catalog */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        data: components["schemas"]["WebhookEventDescriptor"][];
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    get_webhook: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                webhook_id: components["parameters"]["WebhookId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Subscription */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebhookSubscription"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    delete_webhook: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                webhook_id: components["parameters"]["WebhookId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebhookDeleted"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    update_webhook: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                webhook_id: components["parameters"]["WebhookId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["WebhookSubscriptionUpdate"];
            };
        };
        responses: {
            /** @description Updated subscription */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebhookSubscription"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    rotate_webhook_secret: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                webhook_id: components["parameters"]["WebhookId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The new secret (the subscription itself is unchanged) */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebhookSecret"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    test_webhook: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                webhook_id: components["parameters"]["WebhookId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Delivery attempt result */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebhookTestResult"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    list_webhook_deliveries: {
        parameters: {
            query?: {
                /** @description Opaque `next_cursor` from the previous page. */
                cursor?: components["parameters"]["Cursor"];
                limit?: components["parameters"]["Limit"];
                /** @description `delivered` — `delivered_at` set; `dead` — no further attempts; `pending` — still waiting for `next_attempt_at`. */
                status?: "pending" | "delivered" | "dead";
                event_type?: components["schemas"]["WebhookEventType"];
            };
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                webhook_id: components["parameters"]["WebhookId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Page of deliveries */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebhookDeliveryPage"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    get_webhook_delivery: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                delivery_id: components["parameters"]["DeliveryId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Delivery */
            200: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebhookDeliveryWithPayload"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    redeliver_webhook: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Optional trace id of your own. VDSok keeps only the characters
                 *     `A-Za-z0-9._:-` and the first 64 of them; if anything is left, that
                 *     sanitized value becomes the id of the request — it comes back in the
                 *     `X-Request-ID` response header, appears in the error envelope as
                 *     `error.request_id` and is stored in the request log. If nothing is
                 *     left (empty or entirely stripped) VDSok generates a UUIDv4 instead. A
                 *     malformed header is never an error. Reuse across different requests
                 *     is allowed but makes support harder; the SDKs send a fresh id per
                 *     attempt.
                 */
                "X-Request-ID"?: components["parameters"]["RequestId"];
            };
            path: {
                delivery_id: components["parameters"]["DeliveryId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Queued */
            202: {
                headers: {
                    "X-Request-ID": components["headers"]["X-Request-ID"];
                    "X-RateLimit-Limit": components["headers"]["X-RateLimit-Limit"];
                    "X-RateLimit-Remaining": components["headers"]["X-RateLimit-Remaining"];
                    "X-RateLimit-Reset": components["headers"]["X-RateLimit-Reset"];
                    "X-Sandbox": components["headers"]["X-Sandbox"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebhookRedelivery"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
            413: components["responses"]["PayloadTooLarge"];
            415: components["responses"]["UnsupportedMediaType"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["ServerError"];
            502: components["responses"]["BadGateway"];
            503: components["responses"]["ServiceUnavailable"];
            504: components["responses"]["GatewayTimeout"];
        };
    };
    webhook_server_created: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["ServerEvent"];
        responses: {
            "2XX": components["responses"]["WebhookAck"];
        };
    };
    webhook_server_suspended: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["ServerEvent"];
        responses: {
            "2XX": components["responses"]["WebhookAck"];
        };
    };
    webhook_server_unsuspended: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["ServerEvent"];
        responses: {
            "2XX": components["responses"]["WebhookAck"];
        };
    };
    webhook_server_terminated: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["ServerEvent"];
        responses: {
            "2XX": components["responses"]["WebhookAck"];
        };
    };
    webhook_server_reinstalled: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["ServerEvent"];
        responses: {
            "2XX": components["responses"]["WebhookAck"];
        };
    };
    webhook_invoice_created: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["InvoiceEvent"];
        responses: {
            "2XX": components["responses"]["WebhookAck"];
        };
    };
    webhook_invoice_paid: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["InvoiceEvent"];
        responses: {
            "2XX": components["responses"]["WebhookAck"];
        };
    };
    webhook_invoice_overdue: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["InvoiceEvent"];
        responses: {
            "2XX": components["responses"]["WebhookAck"];
        };
    };
    webhook_balance_low: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["BalanceEvent"];
        responses: {
            "2XX": components["responses"]["WebhookAck"];
        };
    };
    webhook_domain_registered: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["DomainEvent"];
        responses: {
            "2XX": components["responses"]["WebhookAck"];
        };
    };
    webhook_domain_expiring: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["DomainEvent"];
        responses: {
            "2XX": components["responses"]["WebhookAck"];
        };
    };
    webhook_domain_renewed: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["DomainEvent"];
        responses: {
            "2XX": components["responses"]["WebhookAck"];
        };
    };
    webhook_key_created: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["KeyEvent"];
        responses: {
            "2XX": components["responses"]["WebhookAck"];
        };
    };
    webhook_key_revoked: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["KeyEvent"];
        responses: {
            "2XX": components["responses"]["WebhookAck"];
        };
    };
    webhook_ping: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: components["requestBodies"]["PingEvent"];
        responses: {
            "2XX": components["responses"]["WebhookAck"];
        };
    };
}
